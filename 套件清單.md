# React 專案套件清單

> 本文件記錄專案所需的所有套件與工具，分為必選與建議選項。

---

## 必選套件

### 1. React 核心技術
- **react** (^18.0.0) - React 核心庫
- **react-dom** (^18.0.0) - React DOM 渲染
- **typescript** (^5.0.0) - TypeScript 編譯器
- **@types/react** - React TypeScript 型別定義
- **@types/react-dom** - React DOM TypeScript 型別定義

### 2. 建置工具與開發環境
- **vite** - 現代化建置工具
- **@vitejs/plugin-react** - Vite React 插件
- **eslint** - 程式碼檢查工具
- **prettier** - 程式碼格式化工具
- **@typescript-eslint/eslint-plugin** - TypeScript ESLint 插件
- **@typescript-eslint/parser** - TypeScript ESLint 解析器

### 3. 路由管理
- **react-router-dom** (^6.0.0) - React 路由管理

### 4. 狀態管理
- **@reduxjs/toolkit** - Redux Toolkit
- **react-redux** - React Redux 綁定

### 5. 資料獲取
- **axios** - HTTP 客戶端
- **@tanstack/react-query** - React Query（伺服器狀態管理）

### 6. 表單處理
- **react-hook-form** - 高效能表單處理
- **zod** - 表單驗證（或使用 yup）

### 7. UI 框架與樣式
- **tailwindcss** - Tailwind CSS 框架
- **postcss** - CSS 後處理器
- **autoprefixer** - CSS 自動前綴

### 8. 測試
- **vitest** - 測試框架（或使用 jest）
- **@testing-library/react** - React 元件測試
- **@testing-library/jest-dom** - Jest DOM 擴充
- **@testing-library/user-event** - 使用者事件模擬

---

## 建議選項（依專案需求選擇）

### UI 元件庫（擇一或組合使用）
- **@mui/material** - Material-UI 元件庫
- **antd** - Ant Design 元件庫
- **@headlessui/react** - Headless UI 元件

### CSS-in-JS（擇一）
- **styled-components** - Styled Components
- **@emotion/react** - Emotion
- **@emotion/styled** - Emotion Styled

### 狀態管理（額外選項）
- **zustand** - 輕量級狀態管理庫
- **jotai** - 原子化狀態管理

### 資料獲取（額外選項）
- **swr** - 資料獲取庫（React Query 的替代方案）

### 表單處理（額外選項）
- **formik** - 表單管理庫
- **yup** - 表單驗證（React Hook Form 的替代方案）

### 動畫
- **framer-motion** - 動畫庫

### 工具函式
- **lodash** - 工具函式庫
- **date-fns** - 日期處理庫（或使用 dayjs）
- **dayjs** - 輕量級日期處理庫

### UI 增強
- **react-icons** - 圖示庫
- **react-toastify** - 通知提示
- **react-helmet-async** - SEO 管理

### 國際化
- **react-i18next** - 多語言支援
- **i18next** - i18n 核心庫

### Git Hooks
- **husky** - Git Hooks 管理
- **lint-staged** - 暫存檔案檢查

### 其他工具
- **react-error-boundary** - 錯誤邊界處理
- **react-window** - 虛擬滾動（大量資料）
- **react-intersection-observer** - 交集觀察器（懶載入）

---

## 安裝指令參考

### 必選套件安裝
```bash
# React 核心
npm install react react-dom
npm install -D typescript @types/react @types/react-dom

# 建置工具
npm install -D vite @vitejs/plugin-react
npm install -D eslint prettier @typescript-eslint/eslint-plugin @typescript-eslint/parser

# 路由與狀態管理
npm install react-router-dom @reduxjs/toolkit react-redux

# 資料獲取
npm install axios @tanstack/react-query

# 表單處理
npm install react-hook-form zod

# 樣式
npm install -D tailwindcss postcss autoprefixer

# 測試
npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event
```

### 建議套件安裝範例
```bash
# UI 元件庫（擇一）
npm install @mui/material @emotion/react @emotion/styled
# 或
npm install antd

# 動畫
npm install framer-motion

# 工具
npm install react-icons react-toastify date-fns lodash

# Git Hooks
npm install -D husky lint-staged
```

---

## 套件版本管理

建議使用 `package.json` 的 `engines` 欄位指定 Node.js 版本：
```json
{
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
```

---

## 更新記錄

- 建立日期：待填寫
- 最後更新：待填寫
