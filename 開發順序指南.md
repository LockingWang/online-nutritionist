# 開發順序指南

> 本文件提供詳細的開發順序，幫助您有條理地完成專案開發與測試。

---

## 開發原則

1. **先後端，後前端** - 先建立穩定的 API，再開發前端介面
2. **先基礎，後進階** - 先完成核心功能，再實作進階功能
3. **持續測試** - 每個階段完成後進行測試
4. **逐步迭代** - 每個功能完成後再進行下一個

---

## 階段一：環境設定與專案初始化（1-2 天）

### 1.1 後端環境設定

#### 步驟 1：建立後端專案
```bash
# 建立後端目錄
mkdir backend
cd backend

# 初始化 npm 專案
npm init -y

# 安裝核心依賴
npm install express prisma @prisma/client
npm install jsonwebtoken bcrypt zod cors dotenv
npm install openai  # AI 功能

# 安裝開發依賴
npm install -D typescript @types/node @types/express @types/jsonwebtoken @types/bcrypt @types/cors ts-node nodemon
npm install -D @typescript-eslint/eslint-plugin @typescript-eslint/parser eslint prettier
```

#### 步驟 2：設定 TypeScript
建立 `tsconfig.json`：
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020"],
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "moduleResolution": "node"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

#### 步驟 3：設定 PostgreSQL 資料庫
```bash
# 安裝 PostgreSQL（Mac）
brew install postgresql@15
brew services start postgresql@15

# 建立資料庫
createdb nutrition_db

# 或使用 Docker
docker run --name postgres-nutrition \
  -e POSTGRES_PASSWORD=yourpassword \
  -e POSTGRES_DB=nutrition_db \
  -p 5432:5432 \
  -d postgres:15
```

#### 步驟 4：初始化 Prisma
```bash
npx prisma init
```

#### 步驟 5：建立專案結構
```
backend/
├── src/
│   ├── controllers/
│   ├── services/
│   ├── routes/
│   ├── middleware/
│   ├── utils/
│   ├── config/
│   └── app.ts
├── prisma/
├── tests/
├── .env
└── package.json
```

#### 步驟 6：設定環境變數
建立 `.env`：
```env
DATABASE_URL="postgresql://user:password@localhost:5432/nutrition_db"
JWT_SECRET="your_super_secret_jwt_key_here"
JWT_EXPIRES_IN="7d"
PORT=3000
NODE_ENV=development
OPENAI_API_KEY="your_openai_api_key_here"
```

#### 步驟 7：設定 package.json scripts
```json
{
  "scripts": {
    "dev": "nodemon src/app.ts",
    "build": "tsc",
    "start": "node dist/app.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio"
  }
}
```

**✅ 檢查點：**
- [x] 後端專案結構建立完成
- [x] TypeScript 設定完成
- [x] PostgreSQL 資料庫連線成功（需要建立 .env 並設定 DATABASE_URL 後測試）
- [x] Prisma 初始化完成（schema.prisma 已建立，待執行 migrate）
- [x] 環境變數設定完成（相關文件與驗證工具已建立，需手動建立 .env 檔案）

---

### 1.2 前端環境設定

#### 步驟 1：建立前端專案
```bash
# 回到專案根目錄
cd ..

# 使用 Vite 建立 React + TypeScript 專案
npm create vite@latest frontend -- --template react-ts
cd frontend

# 安裝核心依賴
npm install react-router-dom @reduxjs/toolkit react-redux
npm install axios @tanstack/react-query
npm install react-hook-form zod
npm install -D tailwindcss postcss autoprefixer
npm install react-icons react-toastify

# 安裝開發依賴
npm install -D @typescript-eslint/eslint-plugin @typescript-eslint/parser eslint prettier
npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event
```

#### 步驟 2：設定 Tailwind CSS
```bash
npx tailwindcss init -p
```

#### 步驟 3：建立專案結構
```
frontend/
├── src/
│   ├── components/
│   │   ├── common/
│   │   └── features/
│   ├── pages/
│   ├── hooks/
│   ├── store/
│   ├── services/
│   ├── utils/
│   ├── types/
│   ├── constants/
│   └── styles/
├── .env
└── package.json
```

#### 步驟 4：設定環境變數
建立 `.env`：
```env
VITE_API_URL=http://localhost:3000/api
```

**✅ 檢查點：**
- [x] 前端專案結構建立完成
- [x] Tailwind CSS 設定完成
- [x] 環境變數設定完成
- [x] 開發伺服器可以正常啟動

---

## 階段二：後端基礎架構（2-3 天）

### 2.1 資料庫設計與遷移

#### 步驟 1：定義 Prisma Schema
編輯 `prisma/schema.prisma`，定義所有資料模型（參考後端架構規劃.md）

#### 步驟 2：執行資料庫遷移
```bash
npx prisma migrate dev --name init
npx prisma generate
```

**✅ 檢查點：**
- [x] 所有資料表建立成功
- [x] Prisma Client 生成成功
- [x] 可以使用 Prisma Studio 查看資料

### 2.2 建立基礎架構

#### 步驟 1：建立回應格式工具
建立 `src/utils/response.ts`：
- `successResponse()` ✅
- `errorResponse()` ✅
- `paginatedResponse()` ✅

#### 步驟 2：建立錯誤處理中介層
建立 `src/middleware/errorHandler.ts` ✅

#### 步驟 3：建立 Express 應用程式
建立 `src/app.ts`：✅
- 設定 CORS ✅
- 設定 JSON 解析 ✅
- 設定路由（待實作）
- 設定錯誤處理 ✅

#### 步驟 4：建立資料庫連線
建立 `src/config/database.ts` ✅

**✅ 檢查點：**
- [ ] 後端伺服器可以啟動（需要建立 .env 並設定 DATABASE_URL）
- [x] 基礎中介層運作正常（errorHandler, authMiddleware, validateRequest 已建立）
- [ ] 資料庫連線成功（需要建立 .env 並設定 DATABASE_URL 後測試）

---

## 階段三：後端核心功能（3-4 天）

### 3.1 認證系統

#### 步驟 1：建立認證服務
- `src/services/authService.ts`
  - `register()`
  - `login()`
  - `hashPassword()`
  - `verifyPassword()`

#### 步驟 2：建立 JWT 工具
- `src/utils/jwt.ts` ✅
  - `generateToken()` ✅
  - `verifyToken()` ✅

#### 步驟 3：建立認證中介層
- `src/middleware/authMiddleware.ts` ✅

#### 步驟 4：建立認證路由
- `src/routes/authRoutes.ts`
  - `POST /api/auth/register`
  - `POST /api/auth/login`
  - `POST /api/auth/logout`
  - `GET /api/auth/me`

#### 步驟 5：測試認證 API
使用 Postman 或 curl 測試：
```bash
# 測試註冊
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"password123","name":"Test User"}'

# 測試登入
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"password123"}'
```

**✅ 檢查點：**
- [ ] 註冊功能正常
- [ ] 登入功能正常
- [ ] JWT Token 生成正確
- [ ] 認證中介層運作正常

---

### 3.2 使用者與個人資料 API

#### 步驟 1：建立使用者服務
- `src/services/userService.ts`
  - `getUserById()`
  - `updateUser()`
  - `updateBodyComposition()`
  - `updateGoal()`

#### 步驟 2：建立營養計算工具
- `src/utils/calculateTDEE.ts` ✅
  - `calculateBMR()` ✅
  - `calculateTDEE()` ✅
  - `calculateNutritionRequirements()` ✅

#### 步驟 3：建立使用者路由
- `src/routes/userRoutes.ts`
  - `GET /api/users/me`
  - `PUT /api/users/me`
  - `PUT /api/users/me/body-composition`
  - `PUT /api/users/me/goals`
  - `GET /api/users/me/nutrition-requirements`

#### 步驟 4：測試使用者 API

**✅ 檢查點：**
- [ ] 取得使用者資料正常
- [ ] 更新使用者資料正常
- [ ] 身體組成計算正確
- [ ] 營養需求計算正確

---

### 3.3 食物資料庫 API

#### 步驟 1：建立食物服務
- `src/services/foodService.ts`
  - `searchFoods()`
  - `getFoodById()`
  - `createCustomFood()`

#### 步驟 2：建立食物路由
- `src/routes/foodRoutes.ts`
  - `GET /api/foods`
  - `GET /api/foods/:id`
  - `POST /api/foods/custom`

#### 步驟 3：匯入基礎食物資料（可選）
建立種子資料或使用外部 API

**✅ 檢查點：**
- [ ] 搜尋食物功能正常
- [ ] 取得食物詳情正常
- [ ] 建立自訂食物正常

---

### 3.4 飲食記錄 API

#### 步驟 1：建立飲食記錄服務
- `src/services/foodLogService.ts`
  - `getFoodLogs()`
  - `getFoodLogById()`
  - `createFoodLog()`
  - `updateFoodLog()`
  - `deleteFoodLog()`
  - `getDailySummary()`

#### 步驟 2：建立營養計算工具
- `src/utils/calculateNutrition.ts`
  - `calculateFoodNutrition()`
  - `calculateDailyNutrition()`

#### 步驟 3：建立飲食記錄路由
- `src/routes/foodLogRoutes.ts`
  - `GET /api/food-logs`
  - `GET /api/food-logs/:id`
  - `POST /api/food-logs`
  - `PUT /api/food-logs/:id`
  - `DELETE /api/food-logs/:id`
  - `GET /api/food-logs/daily-summary/:date`

#### 步驟 4：測試飲食記錄 API

**✅ 檢查點：**
- [ ] CRUD 操作正常
- [ ] 營養素計算正確
- [ ] 每日總結功能正常

---

## 階段四：前端基礎架構（2-3 天）

### 4.1 設定前端基礎

#### 步驟 1：設定路由
- 建立 `src/App.tsx`
- 設定 React Router
- 建立路由守衛（Protected Routes）

#### 步驟 2：設定狀態管理
- 建立 `src/store/store.ts`
- 建立 Redux slices：
  - `authSlice.ts`
  - `userSlice.ts`

#### 步驟 3：設定 API 服務
- 建立 `src/services/api.ts`（Axios 設定）
- 建立 `src/services/authService.ts`

#### 步驟 4：建立基礎 UI 元件
- `Button`
- `Input`
- `Card`
- `Modal`
- `Loading`

**✅ 檢查點：**
- [ ] 路由設定完成
- [ ] 狀態管理運作正常
- [ ] API 服務可以正常呼叫
- [ ] 基礎元件可以正常使用

---

### 4.2 認證功能實作

#### 步驟 1：建立登入頁面
- `src/pages/Login/Login.tsx`
- 使用 React Hook Form
- 使用 Zod 驗證

#### 步驟 2：建立註冊頁面
- `src/pages/Register/Register.tsx`

#### 步驟 3：建立認證 Hook
- `src/hooks/useAuth.ts`

#### 步驟 4：測試認證流程
- 註冊 → 登入 → 登出

**✅ 檢查點：**
- [ ] 註冊流程正常
- [ ] 登入流程正常
- [ ] Token 儲存正確
- [ ] 路由守衛運作正常

---

## 階段五：前端核心功能（4-5 天）

### 5.1 個人資料與目標設定

#### 步驟 1：建立個人資料頁面
- `src/pages/Profile/Profile.tsx`
- 身體組成表單
- 目標設定表單

#### 步驟 2：建立營養需求顯示
- 顯示計算結果
- 視覺化圖表

#### 步驟 3：測試功能

**✅ 檢查點：**
- [ ] 表單提交正常
- [ ] 營養需求計算正確
- [ ] 資料顯示正確

---

### 5.2 飲食記錄功能

#### 步驟 1：建立飲食記錄頁面
- `src/pages/FoodLog/FoodLog.tsx`
- 日期選擇器
- 餐點分類顯示

#### 步驟 2：建立食物搜尋元件
- `src/components/features/food/FoodSearch.tsx`

#### 步驟 3：建立飲食記錄表單
- 新增飲食記錄
- 編輯飲食記錄
- 刪除飲食記錄

#### 步驟 4：建立營養素顯示
- 即時計算
- 進度條顯示

#### 步驟 5：測試功能

**✅ 檢查點：**
- [ ] 新增飲食記錄正常
- [ ] 編輯飲食記錄正常
- [ ] 刪除飲食記錄正常
- [ ] 營養素計算正確
- [ ] 歷史記錄查詢正常

---

### 5.3 本日營養素總結

#### 步驟 1：建立儀表板頁面
- `src/pages/Dashboard/Dashboard.tsx`

#### 步驟 2：建立營養素總結元件
- `src/components/features/nutrition/NutritionSummary.tsx`
- 營養狀態判斷
- 視覺化展示

#### 步驟 3：建立圖表元件
- 使用 Chart.js 或 Recharts
- 圓形圖、長條圖

#### 步驟 4：測試功能

**✅ 檢查點：**
- [ ] 營養素總結顯示正確
- [ ] 狀態判斷正確
- [ ] 圖表顯示正常

---

## 階段六：進階功能（3-4 天）

### 6.1 餐點建議功能

#### 步驟 1：建立餐點建議頁面
- `src/pages/MealSuggestion/MealSuggestion.tsx`

#### 步驟 2：建立餐點推薦邏輯（後端）
- `src/services/mealService.ts`
- `src/routes/mealRoutes.ts`

#### 步驟 3：整合前端
- 顯示推薦餐點
- 餐點詳情

#### 步驟 4：測試功能

**✅ 檢查點：**
- [ ] 餐點推薦正常
- [ ] 推薦邏輯合理

---

### 6.2 AI 功能整合

#### 步驟 1：建立 AI 服務（後端）
- `src/services/aiChatService.ts`
- `src/services/aiAnalysisService.ts`
- `src/services/aiRecommendationService.ts`

#### 步驟 2：建立 AI 路由
- `src/routes/aiRoutes.ts`

#### 步驟 3：建立 AI 聊天介面（前端）
- `src/components/features/ai/AIChat.tsx`

#### 步驟 4：整合 AI 分析
- 營養分析
- 餐點推薦

#### 步驟 5：測試 AI 功能

**✅ 檢查點：**
- [ ] AI 聊天正常
- [ ] AI 分析正常
- [ ] AI 推薦正常

---

### 6.3 統計分析功能（可選）

#### 步驟 1：建立統計 API（後端）
- `src/routes/statisticsRoutes.ts`

#### 步驟 2：建立統計頁面（前端）
- `src/pages/Statistics/Statistics.tsx`
- 趨勢圖表

#### 步驟 3：測試功能

**✅ 檢查點：**
- [ ] 統計資料正確
- [ ] 圖表顯示正常

---

## 階段七：優化與完善（2-3 天）

### 7.1 效能優化

#### 步驟 1：前端優化
- 程式碼分割（Code Splitting）
- 元件記憶化（React.memo）
- 虛擬滾動（大量列表）
- 圖片懶載入

#### 步驟 2：後端優化
- API 快取
- 資料庫查詢優化
- AI 回應快取

### 7.2 響應式設計

#### 步驟 1：手機版優化
- 調整佈局
- 優化觸控體驗

#### 步驟 2：平板版適配
- 調整元件大小
- 優化排版

### 7.3 錯誤處理

#### 步驟 1：前端錯誤處理
- 錯誤邊界（Error Boundary）
- 錯誤提示（Toast）

#### 步驟 2：後端錯誤處理
- 統一錯誤回應
- 錯誤日誌

### 7.4 載入狀態

#### 步驟 1：前端載入狀態
- Loading 元件
- Skeleton Screen

---

## 階段八：測試（2-3 天）

### 8.1 後端測試

#### 步驟 1：單元測試
- 測試服務層
- 測試工具函式

#### 步驟 2：整合測試
- 測試 API 端點
- 測試資料庫操作

### 8.2 前端測試

#### 步驟 1：元件測試
- 測試 UI 元件
- 測試表單驗證

#### 步驟 2：整合測試
- 測試使用者流程
- 測試 API 整合

### 8.3 端對端測試（可選）

#### 步驟 1：使用 Playwright 或 Cypress
- 測試完整使用者流程

---

## 階段九：部署（1-2 天）

### 9.1 後端部署

#### 步驟 1：準備部署
- 設定生產環境變數
- 建置專案

#### 步驟 2：部署到 Railway/Render
- 設定資料庫
- 部署應用程式

### 9.2 前端部署

#### 步驟 1：準備部署
- 設定生產環境變數
- 建置專案

#### 步驟 2：部署到 Vercel/Netlify
- 連接 Git 倉庫
- 自動部署

### 9.3 文件撰寫

#### 步驟 1：API 文件
- 使用 Swagger 或手動撰寫

#### 步驟 2：README
- 專案說明
- 安裝步驟
- 使用說明

---

## 開發檢查清單

### 每日檢查
- [ ] 程式碼符合 ESLint 規範
- [ ] 程式碼已格式化（Prettier）
- [ ] Git commit 訊息清楚
- [ ] 功能測試通過

### 每階段檢查
- [ ] 功能運作正常
- [ ] 錯誤處理完善
- [ ] 程式碼註解清楚
- [ ] 型別定義完整

### 最終檢查
- [ ] 所有功能完成
- [ ] 測試通過
- [ ] 文件完整
- [ ] 部署成功

---

## 開發建議

### 1. 版本控制
- 每個功能完成後立即 commit
- 使用清楚的 commit 訊息
- 定期 push 到遠端倉庫

### 2. 測試優先
- 每個功能完成後立即測試
- 不要累積太多未測試的程式碼

### 3. 程式碼品質
- 遵循 TypeScript 最佳實踐
- 保持程式碼簡潔
- 適當的註解

### 4. 錯誤處理
- 預期所有可能的錯誤
- 提供清楚的錯誤訊息
- 記錄錯誤日誌

### 5. 效能考量
- 注意資料庫查詢效能
- 避免不必要的重新渲染
- 適當使用快取

---

## 時程預估

| 階段 | 預計時間 | 累計時間 |
|------|---------|---------|
| 階段一：環境設定 | 1-2 天 | 1-2 天 |
| 階段二：後端基礎架構 | 2-3 天 | 3-5 天 |
| 階段三：後端核心功能 | 3-4 天 | 6-9 天 |
| 階段四：前端基礎架構 | 2-3 天 | 8-12 天 |
| 階段五：前端核心功能 | 4-5 天 | 12-17 天 |
| 階段六：進階功能 | 3-4 天 | 15-21 天 |
| 階段七：優化與完善 | 2-3 天 | 17-24 天 |
| 階段八：測試 | 2-3 天 | 19-27 天 |
| 階段九：部署 | 1-2 天 | 20-29 天 |
| **總計** | **20-29 天** | - |

---

## 更新記錄

- 建立日期：2024年
- 最後更新：2024年
